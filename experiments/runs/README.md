# Run artifacts

Manifest-driven harness runs create one timestamped directory here containing
the manifest, fully resolved A/B options, cohort and source-state fingerprints,
line-buffered progress, and a machine-readable result. Generated directories
are ignored; preserve evidence intended for review in durable artifact storage
or as a checked-in compact result.
