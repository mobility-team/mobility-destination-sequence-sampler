schema_version = 1
id = "local-pricing-router-runtime-validation"
kind = "quality_runtime"
stage = "validation"
hypothesis = "Local pair escalation retains its validated uniform-8 quality while adding no more than 3% calibrated wall time relative to the current depth-routed 4/8 policy."
mechanism = "The exact 4x4 probe expands only when a candidate enters the working top-K, reducing pair evaluations enough to offset generating wider conditional columns at shallower priced depths."
falsifier = "Reject the router if linked quality regresses or the paired median wall-time regression exceeds 3%."
unknowns = ["A 20,000-context calibrated cohort may not capture every operating-system and cache effect of the full 81,844-context workload."]

[comparison]
allowed_differences = ["pricing_pair_deep_min_layers"]

[cohort]
name = "local-pricing-router-runtime-validation"
role = "validation"
selection_seed = 141421
selector = "calibrated"
contexts = 20000
expected_fingerprint = "8be5df0093ed6d76b61f"

[gates]
max_wall_regression = 0.03
stratified_mass_delta_min = 0.0
zero_mass_delta_max = 0.0

[evidence]
quality_artifact = "../runs/20260727T145848035950Z-local-pricing-router-quality-validation-56d1723a/result.json"

[baseline.options]
frontier_width = 40
proposal_limit_per_source = 16
symmetric_message_limit = 4
symmetric_state_limit = 4
symmetric_forward_proposal_limit = 20
candidate_strategy = "symmetric_factor_map"
surface_bins = 2
factor_map_max_depth = 5
stitch_bias = 1
continuation_state_limit = 1
deep_continuation_state_limit = 2
continuation_proposal_limit = 1
seam_refresh_per_prefix = 1
pricing_passes = 2
pricing_seed_limit = 10
pricing_column_limit = 4
pricing_pair_candidate_limit = 4
pricing_pair_deep_candidate_limit = 8
pricing_pair_deep_min_layers = 9
pricing_next_pass_min_new = 3
pricing_min_layers = 6

[candidate.options]
frontier_width = 40
proposal_limit_per_source = 16
symmetric_message_limit = 4
symmetric_state_limit = 4
symmetric_forward_proposal_limit = 20
candidate_strategy = "symmetric_factor_map"
surface_bins = 2
factor_map_max_depth = 5
stitch_bias = 1
continuation_state_limit = 1
deep_continuation_state_limit = 2
continuation_proposal_limit = 1
seam_refresh_per_prefix = 1
pricing_passes = 2
pricing_seed_limit = 10
pricing_column_limit = 4
pricing_pair_candidate_limit = 4
pricing_pair_deep_candidate_limit = 8
pricing_pair_deep_min_layers = 0
pricing_next_pass_min_new = 3
pricing_min_layers = 6
