schema_version = 1
id = "adaptive-factor-map-runtime-validation"
kind = "quality_runtime"
stage = "validation"
hypothesis = "The locally routed factor-map policy retains its validated quality while reducing calibrated wall time by at least 3%."
mechanism = "Contexts made only of isolated variables skip the redundant partial reverse pass, reducing full-domain factor-map scans and reverse-prefix scoring while hard local and repeated-anchor structures keep symmetric guidance."
falsifier = "Reject the router if linked quality fails or paired median wall time improves by less than 3% on the locked 20,000-context cohort."
unknowns = ["The calibrated cohort may understate scheduler and cache effects on the full 81,844-context workload."]

[comparison]
allowed_differences = ["candidate_strategy"]

[cohort]
name = "adaptive-factor-map-runtime-validation"
role = "validation"
selection_seed = 387298
selector = "calibrated"
contexts = 20000
expected_fingerprint = "a6478c790b4ac93e0fce"

[gates]
max_wall_regression = 0.0
stratified_mass_delta_min = 0.0
zero_mass_delta_max = 0.0

[evidence]
quality_artifact = "../runs/20260727T163303508903Z-adaptive-factor-map-quality-validation-ecc190da/result.json"

[baseline.options]
frontier_width = 40
proposal_limit_per_source = 16
symmetric_message_limit = 4
symmetric_state_limit = 4
symmetric_forward_proposal_limit = 20
candidate_strategy = "symmetric_factor_map"
factor_map_max_depth = 5
stitch_bias = 1
continuation_state_limit = 1
deep_continuation_state_limit = 2
continuation_proposal_limit = 1
seam_refresh_per_prefix = 1
pricing_passes = 2
pricing_seed_limit = 10
pricing_column_limit = 4
pricing_pair_candidate_limit = 4
pricing_pair_deep_candidate_limit = 8
pricing_pair_deep_min_layers = 0
pricing_next_pass_min_new = 3
pricing_min_layers = 6

[candidate.options]
frontier_width = 40
proposal_limit_per_source = 16
symmetric_message_limit = 4
symmetric_state_limit = 4
symmetric_forward_proposal_limit = 20
candidate_strategy = "adaptive_factor_map"
factor_map_max_depth = 5
stitch_bias = 1
continuation_state_limit = 1
deep_continuation_state_limit = 2
continuation_proposal_limit = 1
seam_refresh_per_prefix = 1
pricing_passes = 2
pricing_seed_limit = 10
pricing_column_limit = 4
pricing_pair_candidate_limit = 4
pricing_pair_deep_candidate_limit = 8
pricing_pair_deep_min_layers = 0
pricing_next_pass_min_new = 3
pricing_min_layers = 6
