schema_version = 1
id = "local-pricing-router-quality-validation"
kind = "quality_only"
stage = "validation"
hypothesis = "Local 4x4-to-8x8 escalation preserves or improves post-stratified Mass@10 and does not increase certified zero-overlap cases relative to the current depth-routed 4/8 policy."
mechanism = "A pair expands only when its exact 4x4 neighborhood places a candidate above the current working Kth score, concentrating the wider search on unresolved local pressure."
falsifier = "Reject the router if post-stratified Mass@10 decreases or certified zero-overlap cases increase on the locked validation cohort."
unknowns = ["The exact oracle may leave enough deep validation contexts unresolved for the missingness interval to remain decision-relevant."]

[comparison]
allowed_differences = ["pricing_pair_deep_min_layers"]

[cohort]
name = "local-pricing-router-quality-validation"
role = "validation"
selection_seed = 282843
selector = "stratified"
contexts_per_stratum = 10
expected_fingerprint = "7f45cd8960cff7f65012"

[gates]
stratified_mass_delta_min = 0.0
zero_mass_delta_max = 0.0

[evidence]
quality_artifact = ""

[baseline.options]
frontier_width = 40
proposal_limit_per_source = 16
symmetric_message_limit = 4
symmetric_state_limit = 4
symmetric_forward_proposal_limit = 20
candidate_strategy = "symmetric_factor_map"
surface_bins = 2
factor_map_max_depth = 5
stitch_bias = 1
continuation_state_limit = 1
deep_continuation_state_limit = 2
continuation_proposal_limit = 1
seam_refresh_per_prefix = 1
pricing_passes = 2
pricing_seed_limit = 10
pricing_column_limit = 4
pricing_pair_candidate_limit = 4
pricing_pair_deep_candidate_limit = 8
pricing_pair_deep_min_layers = 9
pricing_next_pass_min_new = 3
pricing_min_layers = 6

[candidate.options]
frontier_width = 40
proposal_limit_per_source = 16
symmetric_message_limit = 4
symmetric_state_limit = 4
symmetric_forward_proposal_limit = 20
candidate_strategy = "symmetric_factor_map"
surface_bins = 2
factor_map_max_depth = 5
stitch_bias = 1
continuation_state_limit = 1
deep_continuation_state_limit = 2
continuation_proposal_limit = 1
seam_refresh_per_prefix = 1
pricing_passes = 2
pricing_seed_limit = 10
pricing_column_limit = 4
pricing_pair_candidate_limit = 4
pricing_pair_deep_candidate_limit = 8
pricing_pair_deep_min_layers = 0
pricing_next_pass_min_new = 3
pricing_min_layers = 6
